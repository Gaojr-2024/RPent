# User documentation flow

## Operation

1. Update the paired English and Chinese LIBERO usage pages.
2. Document separate DreamZero/Cosmos environments and bridge startup order.
3. Provide the RPent `--wam-backend` and `--wam-endpoint` examples.
4. State that Cosmos Predict2 2B LIBERO is executable and DreamZero-DROID is
   connection/prediction-only for this environment.
5. Keep GPU and dependency claims tied to the pinned upstream revision.

## Status

Complete in the paired English and Chinese LIBERO usage pages. Sphinx validation
was not run because `sphinx-build` is unavailable in the local environments.
